# تشغيل وتثبيت نظام إدارة المبيعات

## التشغيل على جهاز جديد

1. نزّل المشروع أو استنسخه من GitHub.
2. افتح الطرفية داخل مجلد المشروع.
3. ثبّت المكتبات:

   ```bash
   pnpm install
   ```

   ويمكن استخدام `npm install` إذا لم يكن pnpm متوفرًا.

4. شغّل المعاينة المحلية:

   ```bash
   pnpm preview
   ```

5. افتح الرابط:

   ```text
   http://127.0.0.1:4173
   ```

لا تفتح `index.html` مباشرة بصيغة `file://`؛ يجب تشغيله عبر خادم محلي حتى يعمل Service Worker وPWA.

## النشر على GitHub Pages

1. ارفع ملفات المشروع إلى فرع `main` في GitHub.
2. افتح إعدادات المستودع: **Settings → Pages**.
3. اختر **Deploy from a branch** ثم الفرع `main` والمجلد `/ (root)`.
4. بعد اكتمال النشر افتح رابط GitHub Pages عبر HTTPS.

المسارات داخل المشروع نسبية، لذلك يعمل التطبيق سواء نُشر في جذر الموقع أو داخل مسار مستودع GitHub Pages.

## تثبيت التطبيق كـ PWA

1. افتح التطبيق مرة واحدة مع وجود الإنترنت.
2. في Chrome أو Edge اضغط رمز التثبيت في شريط العنوان، أو اختر **تثبيت التطبيق** من قائمة المتصفح.
3. على Android اختر **إضافة إلى الشاشة الرئيسية** أو **تثبيت التطبيق**.
4. بعد التحميل الأول يمكن فتح التطبيق دون إنترنت، وتُحفظ صور ImgBB التي تم تحميلها سابقًا في Cache.

## اتصال Convex

نسخة التطبيق المنشورة تتصل مباشرة بمشروع Convex الحالي `sales-admin-abu-amir` عبر deployment:

```text
befitting-toad-819
```

لا يحتاج مستخدم التطبيق إلى تسجيل الدخول إلى Convex.

تسجيل الدخول مطلوب فقط عند تعديل أو نشر دوال Convex من جهاز تطوير جديد. عندها شغّل:

```bash
npx convex dev --configure existing --once
```

ثم سجّل الدخول واختر المشروع الموجود `sales-admin-abu-amir`. لا تنشئ مشروعًا جديدًا.

## ملاحظات التشغيل

- المنتجات والفئات والفواتير والزبائن تُجلب من Convex عند فتح التطبيق مع الإنترنت.
- IndexedDB يبقى التخزين المحلي الأساسي.
- العمليات المحلية المعلقة تُرسل تلقائيًا عند فتح التطبيق Online أو عند رجوع الإنترنت.
- صور المنتجات تُرفع إلى Convex Storage، ثم يحتفظ Service Worker بالصور المحمّلة للعمل Offline.

## إصدار التطبيق والتحديثات

- رقم الإصدار المنشور موجود في `version.json` ويظهر في آخر تبويب الإعدادات.
- عند نشر إصدار أحدث، حدّث رقم `APP_VERSION` في `script.js` ليطابق `version.json`.
- حدّث رقم ملف `script.js` في `index.html` و`sw.js`، ثم ارفع رقم `CACHE_NAME` في `sw.js`.
- الأجهزة المتصلة بالإنترنت تفحص `version.json` تلقائيًا وتعرض زر **تحديث الآن** عند وجود إصدار أحدث.
- التحديث يحذف ملفات واجهة التطبيق القديمة فقط، ولا يحذف بيانات IndexedDB أو صور المنتجات المخزنة للأوفلاين.
